<Window x:Class="TalosForge.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TalosForge Supervisor"
        Width="980"
        Height="660"
        MinWidth="860"
        MinHeight="520"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource WindowBg}"
        Loaded="Window_Loaded"
        Closing="Window_Closing">
    <DockPanel Margin="14">
        <Border DockPanel.Dock="Top" Background="{StaticResource PanelBg}" CornerRadius="10" Padding="14" Margin="0,0,0,10">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button x:Name="BtnStart" Style="{StaticResource BtnGreen}" Content="Start" Margin="0,0,8,0" Padding="22,10" Click="BtnStart_Click" />
                    <Button x:Name="BtnStop" Style="{StaticResource BtnRed}" Content="Stop" Margin="0,0,8,0" Padding="22,10" Click="BtnStop_Click" />
                    <Button x:Name="BtnRefresh" Style="{StaticResource BtnDefault}" Content="Refresh" Padding="22,10" Click="BtnRefresh_Click" />
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="TalosForge Supervisor" Foreground="White" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock Text="Controls start/stop/status via scripts/supervisor.ps1" Foreground="{StaticResource FgSecondaryBrush}" FontSize="12" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border DockPanel.Dock="Top" Background="{StaticResource PanelBg}" CornerRadius="10" Padding="14" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock x:Name="StatusText" Text="Status: unknown" Foreground="White" FontSize="13" FontWeight="SemiBold" />
                <TextBlock x:Name="SessionText" Text="Session ID: n/a" Foreground="{StaticResource FgSecondaryBrush}" FontSize="12" Margin="0,4,0,0" />
                <TextBlock x:Name="RunDirText" Text="Run Dir: n/a" Foreground="{StaticResource FgSecondaryBrush}" FontSize="12" Margin="0,2,0,0" />
            </StackPanel>
        </Border>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="14" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource PanelBg}" CornerRadius="10" Padding="12">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Managed Processes" Foreground="White" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,10" />
                    <DataGrid x:Name="ProcessGrid" ItemsSource="{Binding Processes}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="100" />
                            <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="70" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" />
                            <DataGridTextColumn Header="Stdout" Binding="{Binding OutLog}" Width="*" />
                            <DataGridTextColumn Header="Stderr" Binding="{Binding ErrLog}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <Border Grid.Row="2" Background="{StaticResource PanelBg}" CornerRadius="10" Padding="12">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Health JSON" Foreground="White" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,10" />
                    <TextBox x:Name="HealthJsonTextBox"
                             IsReadOnly="True"
                             FontFamily="Consolas"
                             FontSize="12"
                             Foreground="{StaticResource FgPrimaryBrush}"
                             Background="{StaticResource InputBg}"
                             BorderBrush="{StaticResource BorderDimBrush}"
                             BorderThickness="1"
                             TextWrapping="NoWrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto" />
                </DockPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>
